VM "Extensive" checks. Each file to be checked must be prefixed with the resulting return code expected from the check.

Example:

`1_some_check.asm`

The binary generated from this check when executed must result in an exit code of `1`

